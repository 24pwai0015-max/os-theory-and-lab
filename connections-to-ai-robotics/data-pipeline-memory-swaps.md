# OS Mechanics in Data Analytics & AI Pipelines

Architectural mapping between OS primitives and data/AI systems:
- Virtual memory paging, swap thrashing, and the Linux Out-Of-Memory (OOM) killer.
- Memory-mapped files (`mmap`) for large model checkpoints and datasets.
- Process isolation vs thread contention in Python runtime environments (GIL, multiprocessing, Celery).

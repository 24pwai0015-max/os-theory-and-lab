# Lab 01: Environment Setup & POSIX Verification

## Objective
Verify the native Linux toolchain (GCC, Make, GDB), enforce strict compilation standards (`-Wall -Wextra -pthread -g`), and trace kernel trap boundaries via POSIX `getpid()`.

## Build & Run
```bash
make clean
make
make run
```
